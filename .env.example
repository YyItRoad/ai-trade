# OpenAI API Settings
OPENAI_API_KEY="your_openai_api_key"
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4"
OPENAI_MODEL_CHEAT_CHECK="gpt-3.5-turbo"

# This key is for external k-line data API if needed
KLINE_API_SECRET_KEY="your_kline_api_secret_key"

# This key is for logging into the web UI
APP_LOGIN_SECRET_KEY="change_me_to_a_strong_password"

# JWT Settings for OAuth2
SECRET_KEY="a_very_secret_key_that_should_be_changed"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Linux.do OAuth Settings (Optional)
LINUXDO_CLIENT_ID=""
LINUXDO_CLIENT_SECRET=""
LINUXDO_SCOPE="read"

# --- Database ---
# These settings are used by Docker Compose to initialize the MySQL container
# and by the application to connect to the database.

# For local development with `start-local.sh`:
# DB_HOST should be 'db' to connect to the Docker container.
# For remote database connection with `start-remote.sh`:
# DB_HOST should be 'host.docker.internal' to connect from the container to a service on your host machine.
# Or it can be the IP/hostname of your remote database server.

DB_ROOT_PASSWORD="a_secure_root_password"
DB_HOST=db
DB_PORT=3306
DB_NAME=ai_trade
DB_USER=ai_trade_user
DB_PASSWORD="a_secure_user_password"

# The DATABASE_URL is constructed from the variables above by the application.
# You do not need to set it manually if the above variables are correct.
# Example format: "mysql://user:password@host:port/dbname"
DATABASE_URL=""

# --- Server Settings ---
HOST="127.0.0.1"
PORT=8000
UVICORN_RELOAD=true