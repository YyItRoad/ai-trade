# 这是一个标准的配置文件模板。请复制此文件为 .env 并填入你的实际配置信息。
# 重要提示: 不要将包含你的密钥的 .env 文件提交到版本控制中。

# --- 数据库连接信息 (二选一) ---

# 方式一: 提供完整的数据库连接字符串 (优先使用)
# 如果您填写了此项，下面的 DB_HOST, DB_PORT 等将被忽略。
# DATABASE_URL="mysql://user:password@host:port/dbname"

# 方式二: 提供分项信息 (如果 DATABASE_URL 未设置，将自动从此构建)
# 在 Docker 环境中，主机名应为 docker-compose.yml 中定义的服务名 (例如 'db')
DB_HOST=db
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_strong_password
DB_NAME=ai_trade_db

# --- 仅用于 Docker Compose 的 MySQL 初始化设置 ---
# 无论使用以上哪种方式，在首次使用 Docker Compose 启动时，以下变量都是必需的，
# 因为它们用于初始化 MySQL 容器本身。
DB_ROOT_PASSWORD=your_very_strong_root_password

# --- 外部服务 API ---

# K线数据 API
KLINE_API_SECRET_KEY="your_kline_api_secret_key"

# 大语言模型 API
OPENAI_API_KEY="your_openai_api_key"
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gemini-2.5-pro"

# --- 应用特定设置 ---

# 应用登录密钥
APP_LOGIN_SECRET_KEY="a_strong_secret_key_for_login"