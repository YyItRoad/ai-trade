# 这是一个示例配置文件。请复制此文件为 .env 并填入你的实际配置信息。
# 重要提示: 不要将包含你的密钥的 .env 文件提交到版本控制中。

# --- 数据库连接信息 ---
# 在 Docker 环境中，主机名应为 docker-compose.yml 中定义的服务名 (例如 'db')
# 如果在本地直接运行，请使用 'localhost' 或 '127.0.0.1'
DB_HOST=db
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_strong_password
DB_NAME=ai_trade_db

# --- 仅用于 Docker Compose 的 MySQL 设置 ---
# 这是 MySQL 容器初始化时使用的 root 密码
DB_ROOT_PASSWORD=your_very_strong_root_password

# --- 外部服务 API ---

# K线数据 API (例如币安)
# 请确保你的 API key 具有读取市场数据的权限
KLINE_API_KEY="your_kline_api_key"
KLINE_API_SECRET="your_kline_api_secret"

# 大语言模型 API (例如 OpenAI)
OPENAI_API_KEY="your_openai_api_key"
OPENAI_API_BASE="https://api.openai.com/v1" # 如果使用代理或第三方服务，请修改此地址

# --- 应用特定设置 ---

# 应用登录密钥
# 这是一个用于访问 /manage 页面的简单密码，请设置为一个难以猜到的字符串
APP_LOGIN_KEY="a_strong_secret_key_for_login"